[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fourdesigner-shm"
version = "1.0.0"
description = "Shared-memory TRS + command ring for 4designer (daemon + TD)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["fourdesigner_shm*"]
